import React from "react";
import { Grid } from "@mui/material";

export const Priceoptions = (props) => {
  const { tab } = props;

  console.log(tab);

  return <Grid className="Price-options"></Grid>;
};
